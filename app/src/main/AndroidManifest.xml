<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.zombiblio">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:allowBackup="true"
        android:icon="@drawable/icone_zombi"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icone_zombi"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        >
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity android:name=".Module1" ></activity>

        <activity android:name=".Intro" ></activity>

        <activity android:name=".ModuleMenu" ></activity>

        <activity android:name=".SelectFiliere" ></activity>

        <activity android:name=".Perdu" ></activity>

        <activity android:name=".Module2" ></activity>

        <activity android:name=".Module4" ></activity>

    </application>


</manifest>